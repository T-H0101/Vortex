import SwiftUI

extension Color {
    static let vortexMaterial = Color(NSColor.windowBackgroundColor)
    static let vortexMaterialSecondary = Color(NSColor.controlBackgroundColor)
    static let vortexAccent = Color.accentColor
    static let vortexText = Color(NSColor.labelColor)
    static let vortexTextSecondary = Color(NSColor.secondaryLabelColor)
    static let vortexSeparator = Color(NSColor.separatorColor)
}